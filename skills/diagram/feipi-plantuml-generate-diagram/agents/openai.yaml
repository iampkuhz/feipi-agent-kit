version: 7
interface:
  display_name: "PlantUML 作图生成与校验"
  short_description: "唯一 PlantUML 入口，支持思维导图自动布局、类型校验与图包输出。"
  default_prompt: "使用 $feipi-plantuml-generate-diagram 做一次批次预检；本地 renderer 不可用时仅尝试一次固定 Podman 启动，mindmap 优先使用专用生成器自动布局，再按图型 brief 生成并校验 PlantUML diagram package。"
