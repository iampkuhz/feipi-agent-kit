# 变更记录与 README 同步

## CHANGELOG 规则
- 每次更新完成后，必须更新仓库根目录 `CHANGELOG.md`。
- 按天分章节，格式 `## YYYY-MM-DD`，时间倒序排列（新日期在上）。
- 记录当次更新的最小要点，避免冗长。
- 更新前先检查今天章节是否已有相同记录，已有则不重复追加。
- 若是继续优化昨天的内容，在昨天对应条目追加“未调试成功”标记；最多只回看一天，更早内容不做修改。

## README 同步
- 每次更新后，检查 `README.md` 是否需要微调（命令、示例、路径等变更）。
