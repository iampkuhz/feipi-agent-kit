# 用户问题与优化历史

> 仅供维护者查阅；不是规则真源、模板或运行参考。执行本 skill 时不得读取或引用本文件。

本文件按反馈轮次记录三件事：你指出了什么问题、这个问题为什么会影响交底质量、skill 最终如何处理。正式执行规则仍以 `SKILL.md`、模板、schema 和校验脚本为准。

状态说明：

- [x] 表示本地规则、模板、脚本及合成测试已经完成。
- [ ] 表示仍未实现，或仍缺真实材料才能完成验收。
- 状态完成不代表专利新颖性、真实业务效果或外部事实已经得到证明。

## 2026-08-09｜第一轮：内容与图示质量

### 1. 组件总览的顶层拆分失真

- [x] **状态：已完成**
- 你指出的问题：总览图把技术载体、共享 JAR、处理器类等实现细节直接当成顶层模块，看起来像在画代码结构，而不是系统边界。
- 主要影响：评审人无法判断哪些是真正独立的业务域、系统或物理端点，也无法理解它们的归属关系。
- 已做优化：画图前先整理业务域、系统归属、拥有方和物理边界；顶层只允许业务域、独立系统和物理端点，实现细节只能进入可选的模块细化图。

### 2. 图面箭头密集、文字重叠

- [x] **状态：已完成**
- 你指出的问题：一张图放入太多节点、关系和说明，箭头交叉，边上文字遮挡。
- 主要影响：即使图包含全部信息，也无法快速阅读，失去“用图解释一个问题”的意义。
- 已做优化：结构图限制为最多 8 个节点、10 条可见边、单节点 4 条连接；边上只保留 `E` 编号，详细说明放在图下；最终 SVG 还要人工确认无交叉和遮挡，并绑定当前文件指纹（hash）。

### 3. 流程图标签重复且过长

- [x] **状态：已完成**
- 你指出的问题：流程箭头同时塞入步骤编号、完整句子、参数和异常说明，同一信息在图内反复出现。
- 主要影响：主流程不突出，步骤编号和说明容易混乱。
- 已做优化：主流程统一使用连续的 `S1...Sn`，子调用使用 `Sx.y`；图内只保留一个动作短语，参数、条件和异常处理统一放在图下对应步骤中。

### 4. 关键词随意、区分度低

- [x] **状态：已完成**
- 你指出的问题：关键词容易写成“平台、系统、安全、高效、智能、优化”等泛词，无法体现发明特征。
- 主要影响：关键词看似齐全，但无法帮助检索和评审，也不能说明方案真正解决了什么。
- 已做优化：固定为 5–8 个关键词，并分成技术对象 1–2 个、核心机制 2–4 个、关键约束 1–2 个；三组不能重复，孤立泛词和内部变量直接拦截。

### 5. 关键创新点停留在普通模块拆分

- [x] **状态：已完成**
- 你指出的问题：把“增加一个模块”“拆成几个组件”直接当成创新点，没有说明机制和约束。
- 主要影响：这种描述只是系统组成，不能解释发明为什么成立。
- 已做优化：先提炼唯一的核心发明主张，再保留 2–4 个创新点；每项都要写核心机制、必要约束、实质差异以及它在正文或图中的具体落点。

### 6. 技术效果缺少因果关系

- [x] **状态：已完成**
- 你指出的问题：效果只写“提升效率、增强安全、优化体验”，没有解释是哪个机制产生了什么结果。
- 主要影响：任何方案都可以套用这些结论，无法证明效果来自本方案。
- 已做优化：为每个创新点建立唯一的 `T` 技术效果，逐项写清原问题、采用机制、可观察结果和验证状态；没有实测数据时只能写“预期可观察”。

### 7. 高概括章节缺少语义门禁

- [x] **状态：已完成**
- 你指出的问题：只要文字读起来通顺，即使换成任意行业仍成立，或者删除核心机制后效果仍成立，也可能通过检查。
- 主要影响：交底书形式完整，但内容可能是空话。
- 已做优化：增加“泛化替换测试”和“因果删除测试”；复核失败时阻塞，尚未复核时标记为 `review_required`，脚本不假装已经理解语义。

### 8. 来源事实、方案扩展和内部实现混在一起

- [x] **状态：已完成**
- 你指出的问题：用户提供的事实、撰写时推导出的方案、外部资料以及类名、字段名等内部实现被混写成同一层信息。
- 主要影响：无法分辨哪些内容有输入依据，哪些内容只是待确认设计，容易把推测写成事实。
- 已做优化：建立“来源事实、发明扩展、外部资料”三类台账；发明扩展必须引用依据，外部资料必须记录来源和日期，内部标识默认转换成通用技术表达。

### 9. 竞品与相似方案缺少证据

- [x] **状态：已完成**
- 你指出的问题：为了满足固定数量而硬写竞品名称、能力或上线时间，但没有可核验来源。
- 主要影响：交底书会出现无法证明的外部断言。
- 已做优化：取消强制凑 2–3 项；有证据时写 1–3 项完整记录，没有可靠证据时明确写“待检索”，不得补写未经证实的竞品能力。
- 后续演进：该轮先解决“虚构竞品”问题；问题 18 进一步取消“待检索”占位，升级为无输入也必须主动检索并记录过程。

### 10. 校验只保格式，不保内容

- [x] **状态：已完成**
- 你指出的问题：旧测试只检查章节、字数和数量，编号混用、创新与效果脱节、来源悬空等问题仍可能全部通过。
- 主要影响：测试显示成功，但交底质量没有真正提升，形成“假绿”。
- 已做优化：交付物升级为正文、manifest、验证报告和图包；共同检查结构、内容字段、编号、来源、`I/T` 映射、图文同源、文件指纹以及人工复核状态。

## 2026-08-09｜第二轮：写作过程与维护记录

### 11. 最终撰写前没有确认写作思路

- [x] **状态：已完成**
- 你提出的要求：撰写过程必须有明确阶段，并且先与你确认写作思路，确认后才能开始最终版本。
- 主要风险：如果直接生成最终稿，核心主张、创新点或图示方向一旦偏离，后续只能整体返工。
- 已做优化：流程分为“素材建模、思路确认、最终撰写、复核交付”四个阶段；思路阶段必须展示核心主张、论证顺序、创新与效果、图示规划和证据缺口；没有收到明确确认时不得生成最终正文、完整 manifest 或最终图包。

### 12. 需要长期保留每次反馈和优化记录

- [x] **状态：已完成**
- 你提出的要求：在 skill 内保存一份自己查看的历史文件，持续记录每次问题和处理方式。
- 已做优化：新增本文件并随 skill 一起维护，但不从 `SKILL.md`、模板或脚本引用，因此不会占用正常执行上下文。

## 2026-08-09｜第三轮：历史可读性与创新价值

### 13. 历史记录过于简略，表格难以阅读

- [x] **状态：已完成**
- 你指出的问题：原记录每项只有一句“问题”和一句“优化摘要”，缺少背景，无法理解为什么要这样改；表格也不适合承载多层解释。
- 已做优化：取消表格，改为按反馈轮次和问题编号组织；每项使用“问题或要求、主要影响、已做优化”的多层列表，保留足够上下文。

### 14. 创新点没有和价值关联

- [x] **状态：已完成**
- 你指出的问题：当前创新点主要说明某个环节“怎么处理”，但没有说明现有做法是什么、与现有做法相比改变了什么，以及这种改变有什么好处。
- 主要影响：创新点会退化成实现步骤。评审人只能看到“我们怎么做”，看不到“为什么这构成差异”和“差异为什么产生价值”。
- 已做优化：每个创新点必须连续写出以下链路：
  1. 对比基线：来源事实支持的现有做法，或不指向具名竞品的通用技术做法。
  2. 本方案处理方式：具体采用什么核心机制。
  3. 实质差异：技术对象、关系、状态或约束相对基线发生了什么变化。
  4. 价值关联：为什么上述差异会产生对应价值，不能只写“更高效”“更安全”。
  5. 可观察效果：通过 `effect_id` 绑定唯一 `T`，说明最终能观察或验证什么结果。
- 落地方式：manifest 新增 `comparison_baseline`、`value_link` 和 `effect_id`；模板把五项放在同一个创新点下连续展示；校验器拦截字段缺失、泛化价值和双向 `I/T` 映射不一致。
- 证据边界：没有外部证据时只能与来源事实中的现有做法或通用技术基线比较，不能为了说明“别人怎么做”而虚构具名竞品能力。

## 2026-08-09｜第四轮：实现基础与保护边界

### 15. 没有区分已实现内容和拟扩展保护内容

- [x] **状态：已完成**
- 你指出的问题：交底书需要明确哪些技术路径已经实现，哪些内容只是希望进一步扩大保护范围；正文应稍微偏向真实实现，但不能原样照搬内部说明。
- 主要影响：如果两类内容混在一起，扩展设想会被误读为已经上线的事实；反过来，完全抽象化又会让交底书脱离真实技术基础。
- 已做优化：每个创新点都必须先填写“已实现基础”，包含技术对象、触发或输入、实际处理、必要边界、输出或状态以及来源事实编号；这些内容保留真实技术路径，但必须泛化类名、函数、字段、表结构和内部产品名。
- 扩展保护方式：每个创新点可以填写零个或多个拟扩展项；每项必须引用发明扩展台账中的唯一 `IE` 编号。写作思路和内部附录保留 `IE` 映射，对外正文只用 `> **拟扩展保护**` 高亮保护范围和扩展理由；没有扩展时不显示空占位块。
- 阶段与图示：写作思路阶段明确展示 `IE` 供内部确认；最终图示只画已实现路径，拟扩展统一留在正文高亮块和内部附录，避免图中重新混淆实现与扩展。
- 验证方式：新增 `INV-007`，拦截缺少实现基础、实现来源悬空、照搬内部标识、`IE` 台账与创新点不一致、存在孤立 `IE`、扩展字段不完整以及扩展正文未真正进入高亮块等情况。
- 验证边界：脚本只能检查分类和来源记录是否完整，不能证明某项功能在真实产品中已经实现；最终分类仍需你在写作思路确认阶段确认。

### 16. 历史问题没有完成状态

- [x] **状态：已完成**
- 你指出的问题：历史文件只能看到问题和优化内容，无法快速判断哪些已经修复、哪些仍未完成。
- 已做优化：每个历史问题增加 `- [x]` 或 `- [ ]` 状态；文末单独保留未完成验收项，避免把规则落地和真实环境验收混为一谈。

## 2026-08-09｜第五轮：内外版本边界

### 17. 最终产出物没有区分对外版本和内部版本

- [x] **状态：已完成**
- 你指出的问题：方案背景、事实来源和创新依据中会直接出现“来源事实 SF1”“依据 SF2”等开头；这些编号用于关联内部事实文档，不应该在对外版本中呈现。
- 主要影响：内部追溯语言会降低正式交底书的可读性，也可能把来源定位、校验状态或内部材料结构一并暴露给外部接收方。
- 已做优化：保留 `disclosure.md` 作为对外版本，正文不显示 `SF/IE/EM/C/BD/SYS/PB` 等台账编号、来源定位、原始内部术语、机器枚举和模板注释；新增 `disclosure-internal.md`，其公开正文与对外版完全一致，只在文末追加“内部追溯附录（禁止对外）”。
- 验证方式：新增 `PKG-011`、`DOC-004`、`DOC-005` 和 `FIG-010`，检查双文件存在、对外内容洁净、两版正文同源、内部附录完整，以及 PlantUML/SVG 只呈现已实现路径；校验前会解码 HTML 实体并规整 Unicode，避免编码后的内部编号或术语绕过检查。

## 2026-08-09｜第六轮：竞品研究完整性

### 18. 缺少竞品输入时直接留空或写“待检索”

- [x] **状态：已完成**
- 你指出的问题：行业竞品分析不能因为用户没有提供竞品材料就什么都不写，也不能只输出“待检索”之类的占位说明；skill 应主动搜索一些相关行业方案。
- 主要影响：竞品章节虽然避免了虚构事实，但没有提供任何行业参照，无法帮助判断本方案与现有产品、公开方案的关系，也会把本应由撰写流程完成的研究工作重新推回给用户。
- 已做优化：无论用户是否提供竞品，都至少执行两组公开资料检索，分别覆盖泛化后的技术对象和核心机制；manifest v1.2 要求记录检索依据词、跨分类或使用场景上下文词、检索式、日期、实际查阅页面和结果摘要，并校验这些技术词与已确认输入、关键词和实际检索式同源。
- 输出方式：找到可靠资料时列出 1–3 项有来源的竞品或相似方案；确实没有可用证据时，也必须展示检索范围、日期和具体结论，不能留空或出现“待检索”。
- 失败处理：检索工具不可用或检索记录不完整时暂停最终成稿，不用占位文本伪装成已完成；本地脚本只验证检索记录是否完整和自洽，不能证明网络搜索真实发生或外部事实绝对正确。
- 验证方式：新增 `INP-004`、`CMP-005` 等稳定规则，覆盖伪造/私网地址、缺少技术上下文的无关检索、HTML entity、零宽字符、输入自污染、重复检索、空摘要、无证据时具名断言和中英文占位符；完整回归 101/101 通过，并用一个未提供竞品名称的独立请求验证能够先主动检索、再停在写作思路确认阶段。
- 能力边界：脚本只能确认检索记录具有已绑定的技术依据、上下文和研究意图，不能判断检索式其余自由文本是否语义相关，也不能证明网络检索真实发生；这两项必须由执行阶段检查实际查询和页面。

## 2026-08-09｜第七轮：输出目录隔离

### 19. 外发稿与内部过程产物混在同一目录

- [x] **状态：已完成**
- 你指出的问题：最终外发版、内部追溯稿、manifest、验证报告和图包平铺在同一个目录，Git 无法只追踪最终产出；希望所有中间产物进入一个可统一加入 `.gitignore` 的固定子目录。
- 主要影响：提交时容易误带内部来源、检索记录和校验工件，也难以通过一条递归忽略规则保持仓库只追踪外发稿。
- 目录名决策：使用 `disclosure-workspace`，不使用 `disclosure-package`。因为最终外发稿位于目录外，`package` 容易被误解为完整交付包；`workspace` 更准确表示内部稿、事实模型、图包和临时过程文件组成的工作区。
- 已做优化：交底书目录根部只保留唯一 `disclosure.md`；内部稿、manifest、validation、图包及所有落盘的检索记录、写作思路和临时草稿统一进入同级 `disclosure-workspace/`。根 `.gitignore` 可使用 `**/disclosure-workspace/`。
- 兼容策略：主校验入口参数仍是交底书目录；旧平铺目录只做带警告的读取兼容，新旧 manifest 同时存在、内部工件泄漏到根部或工作区复制外发稿时直接阻塞。
- 验证方式：新增 `PKG-012`、`PKG-013` 与布局回归，覆盖新布局、旧布局警告、双布局歧义、根部内部文件泄漏、工作区重复外发稿、验证报告落点及工作区符号链接越界；完整回归 107/107 通过。

## 2026-08-24｜第八轮：执行性能与真实观测

### 20. 启动即加载全部资源，分析上下文过重

- [x] **状态：已完成**
- 你指出的问题：skill 执行时间过长，模型在进入素材确认前就读取正文模板、内部模板、manifest 模板和大体量 JSON Schema，形成不必要的前置分析。
- 已做优化：改为按阶段渐进加载。阶段 1 只读取 skill 入口并建立紧凑事实台账；用户确认后才读取写作模板；JSON Schema 只交给确定性脚本；人工复核时才加载对应质量规则。

### 21. PlantUML 渲染和图包校验重复

- [x] **状态：已完成**
- 你指出的问题：同一张图会先请求文本渲染再请求 SVG，图包自校验后完整交底入口又逐图启动 verifier，未变图也可能被再次渲染。
- 已做优化：renderer 成功路径收敛为单次 SVG 请求；未变图包可在当前路径、hash、metrics、profile 与渲染合同均有效时显式复用；专利完整校验在当前进程调用通用 verifier，避免逐图启动 Python 子进程；生成阶段每图只做一次图包校验。
- 后续校准：复用命中单独写入 `last_run_timings` / `last_run_counters`，本次渲染耗时、HTTP 请求和渲染轮次均归零；渲染错误改用 PlantUML 响应头或 SVG 根元数据识别，避免用户图中文字触发误判，并用真实 XML 根元素校验替代 `<svg` 子串扫描。

### 22. 缺少分级 subagent 与真实 Session 耗时证据

- [x] **状态：已完成**
- 你指出的问题：子任务没有按难度分配模型和思考深度，也没有记录阶段、资源读取、检索、制图、渲染、验证、subagent 数量、执行次数与等待时间，后续无法基于真实 Session 判断瓶颈。
- 已做优化：增加 Luna/medium 竞品检索、Terra/medium 制图和 Sol/high 终审三级编排，禁止递归派生和全员最高模型；新增 `session_timing.py`，以 JSONL span 记录四阶段及七类活动，并输出包含 subagent 数量、执行与等待统计的 summary。PlantUML 图包额外写入真实 render/static validation timing。
- 观测边界：活动允许并行，不能把各活动耗时相加当作 Session 墙钟总时长；阶段 2 包含等待用户确认，必须与模型或脚本执行耗时分开解释。
- 后续校准：增加 active session 防覆盖与 `--resume`、四阶段/五类活动完整性门禁、阶段多次执行聚合、宿主 runtime id、请求/实际模型区分，以及 renderer HTTP 请求、渲染轮次、package validation、verifier、cache hit 的实际计数。宿主未暴露模型内部延迟或实际模型时明确记录 `unknown`，不把请求配置或阶段墙钟冒充真实模型计算。

### 23. 阶段交付依赖对话上下文且文件链不明确

- [x] **状态：已完成**
- 你指出的问题：各阶段没有明确原始输入、前序结构化输入、本阶段判断、内部缓存和下一阶段输出；写作思路默认不落盘，跨轮次和 subagent 容易重复读取材料、携带长推理或使用不一致的文件。
- 已做优化：新增四阶段交付矩阵和固定 `working/stages/` 目录。阶段 1 只读一次原始材料并建立材料索引与唯一证据卡；阶段 2–4 只从已封存 handoff 继续，分别缓存确认决策、工件映射和绑定 hash 的复核结论。
- 压缩方式：重复记录统一使用单表头 TSV；handoff 限制为 24 KiB，只传稳定 ID、相对路径、hash、决策和未决项，不复制正文、网页、日志、完整 JSON 或思维链。三个 subagent 均明确“输入 / 需要判断 / 返回”，消息只传任务文件路径和紧凑结果状态。
- 稳定性：新增 `stage_handoff.py` 管理四阶段 hash 链；上游变化会使下游状态失效但保留缓存文件。并行仅允许阶段 3 的正文与 diagrams 不相交写入，汇合前禁止封存、计算最终 manifest hash或启动终审。
- 验证边界：工具验证路径、表头、状态、hash、大小和连续性，不判断专利语义；原始材料是否确实只读一次仍依赖执行 agent 遵守合同，可结合 session timing 的 `resource_read` 记录复核。

## 2026-08-25｜第九轮：关键进展上报

### 24. 主子 Agent 缺少精简的关键事件上报，主 Agent 可能轮询等待

- [x] **状态：已完成**
- 你提出的要求：主 agent 和 subagent 都要在关键事件上报，但展示必须精简；主 agent 不能通过 busy waiting、短间隔等待或轮询 subagent 来制造进度。
- 主要影响：如果没有统一事件定义，agent 容易把“已开始、仍在处理、读取文件、调用工具、缓存命中”等普通过程不断展示；主 agent 还可能反复查询 worker 状态，增加等待、工具调用和上下文 token，却没有产生新的可用结果。
- 关键事件定义：只保留四类：
  1. `MILESTONE`：已经形成可供下游消费的阶段 handoff 或关键工件。
  2. `DECISION`：需要用户或主 agent 作出会改变主张、保护边界、证据范围、图示职责或交付状态的选择。
  3. `BLOCKED`：缺少必要输入、权限、工具能力，或必做门禁失败，当前职责无法继续。
  4. `COMPLETE`：subagent 当前职责或主任务已经闭环。
- 重叠判定：职责全部闭环时用 `COMPLETE`，不再发 `MILESTONE`；接收方选择可以解除停滞时用 `DECISION`，否则才用 `BLOCKED`。未注册事件拒绝对外转发。
- 精简方式：`STARTED`、心跳、空泛状态、工具调用、文件读写、缓存命中、无变化等待和预算内重试全部静默；subagent 只向主 agent 上报一行事件，主 agent 按阶段去重和聚合后，才向你展示一句“结果 + 下一步”。最终响应本身就是 `COMPLETE`，不再重复发完成通知。
- 等待方式：主 agent 派发后先继续可并行的本地工作；只有到依赖汇合点且没有其他工作时，才进行长时、事件驱动等待。禁止用短间隔 `wait`、`list`、`status`、`read` 或 `sleep` 循环查询；宿主非终态超时后可以续接同类长等待，但不得夹带状态查询、心跳或缩短等待周期，也不能因此重派。
- 落地方式：事件语义写入阶段交付合同，编排 JSON 增加主/子上报 profile、静默事件、分类优先级、去重和等待策略，每个 subagent 任务包必须携带统一事件合同；session timing 只记录真实阻塞等待，不把轮询伪装成等待。
- 验证边界：本地测试可以确认声明式合同、角色绑定和禁止轮询配置完整，但不能证明宿主在真实运行中绝不轮询；这仍需要后续真实 Session 回归观察。

## 2026-08-26｜第十轮：阶段检查点与断点恢复

### 25. 阶段和 subagent 缺少可验证的任务检查点

- [x] **状态：已完成**
- 你提出的要求：任务运行时维护 `working/CHECKPOINT.md`；每个阶段开始时记录当前阶段、本阶段输入、本阶段任务、当前阶段已交付、下一步和阻塞项。恢复时先读检查点，跳过仍然有效的已完成任务，从第一个未完成任务继续。
- 主要风险：原有 `stage-state.tsv` 只证明阶段 handoff 的封存链有效，不能表达阶段内部哪些任务已经完成、某个 subagent 的结果是否真正落盘，也不能在中断后精确定位应从哪项任务恢复。只看聊天中的“完成”消息还可能跳过空文件、损坏文件或已经被改写的旧结果。
- 检查点定位：新增 `disclosure-workspace/working/CHECKPOINT.md`，只保存当前状态，不保存过程日志。固定记录当前阶段与状态、本阶段输入、任务清单、当前阶段已交付、下一步和阻塞项；任务粒度限制为一个阶段交付物或一个 subagent 职责，禁止登记文件读取、工具调用、写作微步骤和重复进度。另用机器可读 catalog 固定四阶段及三个 subagent 的 task id、结果路径、最低检查和允许 owner，运行时不能删减或合并，额外任务只能追加。
- 完成门禁：每项任务必须绑定唯一结果文件和最低检查类型。只有文件存在、非空并通过 `nonempty`、`markdown`、`json` 或 `tsv` 检查后，才能勾选完成并加入“当前阶段已交付”；同时记录当前 SHA-256。最低检查失败时不得修改检查点，也不能用 subagent 的聊天消息代替结果文件。
- 恢复方式：恢复命令重新验证已完成结果的存在性、基本格式和 hash；仍有效的任务直接跳过，缺失、空文件、格式无效或 hash 改变的任务重新执行，并返回第一个无效或未完成的 task id。恢复检查本身只读，不产生新的进度记录。
- 更新时机：只在阶段开始、任务完成、等待用户和发生阻塞时原子更新。等待超时、缓存命中、普通重试、subagent 中间事件和工具过程全部静默，避免把检查点重新变成高频日志。
- 与现有机制的关系：CHECKPOINT 负责阶段内任务恢复，`stage_handoff.py` 继续负责四阶段 handoff 的封存和 hash 链。恢复时先检查 CHECKPOINT，再核对最后一个有效 handoff；两者都不能绕过对方的失效结论。
- 回退边界：用户修改输入或冻结计划时，由主 agent 判断回退到阶段 1、2、3 或 4；先重置 CHECKPOINT，再用显式 `stage_handoff.py rewind --stage` 删除该阶段及下游的封存状态但保留缓存，随后重建所选阶段任务。第一版明确不做自动依赖分析；回退范围会改变交付内容且无法判断时，必须请求用户选择。
- subagent 绑定：研究、制图和终审分别绑定 `research.tsv`、不可变的 `diagram-result.tsv` 和 `review.tsv`；主 agent 收到终态事件后仍须先把结构化结果写入约定文件并通过门禁，才能完成对应 checkpoint task。阶段 3 在正文汇合后另行生成最终 `build-map.tsv`，避免追加主工件导致已完成的制图结果 hash 失效。
- 运行时完整性：`start-stage`、`rollback-stage` 和检查点读取都会校验 catalog；固定任务缺失、乱序、结果路径/最低检查不一致或 owner 越权都会失败。并行任务可以乱序完成，但恢复始终按 catalog 顺序选择第一项 pending 或失效任务；等待用户或阻塞状态不会被普通 pending 自动解除。
- 验证边界：本地工具能证明检查点结构、结果文件最低格式和 SHA-256 绑定是否自洽，不能判断交底内容是否正确，也不能替代完整交付包校验、SVG 视觉复核、专利语义审查或真实 Session 恢复演练。

## 尚未完成的验收项

- [ ] **真实 Session 回归**：目前只有合成交底包和独立合成请求回归；在获得脱敏真实 Session 前，不得宣称真实使用场景已经覆盖。
