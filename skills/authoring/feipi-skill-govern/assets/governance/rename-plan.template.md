# Rename Plan

- `current_path`:
- `target_path`:
- `current_name`:
- `target_name`:
- `target_layer`:
- `target_domain`:
- `target_action`:
- `target_object`:
- `rename_reason`:
- `rule_violation`:
- `migration_risk`:

## 需要同步更新的内容

- 路径引用：
- 脚本引用：
- 文档引用：
- 测试入口：
- 版本与 changelog：

## 验证计划

- `script_localization_status`:
- `validation_status`:
- dry-run / 非破坏性验证：
- 回滚方案：
