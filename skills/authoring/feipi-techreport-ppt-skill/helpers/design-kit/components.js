/** Legacy renderer retired: use Semantic IR v2 -> Render Plan -> PptxBackend. */\n'use strict';\n\nfunction retired() { throw new Error('design-kit renderer 已停用，请使用 compiler/render-plan-compiler'); }\n\nmodule.exports = new Proxy({}, { get: () => retired });\n
