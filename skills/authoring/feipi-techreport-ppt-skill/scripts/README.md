# Scripts

This skill currently relies on the underlying `pptx` skill for deterministic PPTX operations, rendering, thumbnail generation, XML inspection, and visual QA support.

Future scripts may include:

- `slide_density_check.py`
- `page_contract_lint.py`
- `ppt_text_budget_check.py`
- `visual_overlap_check.py`

Do not add speculative scripts unless they are implemented and tested.
